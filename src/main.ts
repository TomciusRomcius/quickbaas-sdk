import 'dotenv/config';

// TODO: Check if the backend url is defined
const BACKEND_URL = process.env.BACKEND_URL!;

export class Auth {
  public static signInWithPassword(email: string, password: string) {
    
  }
}
